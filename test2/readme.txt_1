110     char *p = line;
51 a known recursive algorithm, to write all the data to the output file.
11     For the functionality of the server's setup with the fragment files,
69 // TODO: GO BACK AND COMMENT GPT CODE WITH EXPLANATIONS!!!!
119     // DO NOT skip spaces or tabs — we want to keep them
35 When a connection is made, the server accepts it, but before doing anything else,
17 how much space is in the array of pointers. If there is a new line and no space
109 
26 to main().
101 
115         p++;
39 the normal case of the server accepting a valid client, the serve writes work
108     // Suppose you read a line into `line` here
2 
